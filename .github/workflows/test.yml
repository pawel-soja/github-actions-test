name: Test nproc
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, debian-latest]

    steps:
      - name: Get Source
        uses: actions/checkout@v2

      - name: Run
        run: ./scripts/test.sh

